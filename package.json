{
  "name": "hardhat-project",
  "scripts": {
    "test": "npx hardhat test --config hardhat.config.testing.ts",
    "compile": "npx hardhat compile",
    "clean": "npx hardhat clean",
    "format": "biome format ./",
    "lint": "biome check ./",
    "fix:format": "biome format --write ./",
    "fix:lint": "biome check --apply ./"
  },
  "devDependencies": {
    "@biomejs/biome": "1.8.2",
    "@nomicfoundation/hardhat-ethers": "^3.0.6",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "ethers": "^6.13.1",
    "hardhat": "^2.22.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@nilfoundation/hardhat-plugin": "^0.1.0",
    "dotenv": "^16.4.5"
  }
}
